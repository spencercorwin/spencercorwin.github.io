function myFunction() {
  const street = document.getElementById('input').elements[0].value;
  const city = document.getElementById('input').elements[1].value;
  const state = input.elements[2].value;
  const zip = input.elements[3].value;
  document.getElementById('demo').innerHTML = street+"<br>"+city;
  //todo - Look at Python implementation to determine API to use
}
